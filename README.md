## Fake Python Classwork Server


### `npm install -g json-server`
Your should install json-server globally, first.

### `npm start`
I write start script in package.json, you can run the server easily.